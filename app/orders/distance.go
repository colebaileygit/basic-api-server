package orders

import (
	"github.com/colebaileygit/basic-api-server/types"
)

func calculateDistance(params types.PlaceOrderParams) {
	// TODO: implement Google API call

	// url := "http://maps.googleapis.com/maps/api/distancematrix/json?units=metric" +
	// 	"&origins=" + strings.Join(params.Origin, ",") +
	// 	"&destinations=" + strings.Join(params.Destination, ",") +
	// 	"&key=" + os.Getenv("GOOGLE_API_KEY")

	// url = ""
}
